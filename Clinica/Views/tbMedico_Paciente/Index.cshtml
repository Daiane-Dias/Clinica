@model IEnumerable<Clinica.Models.tbMedico_Paciente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create Paciente", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tbPaciente.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbProfissional.IdUser)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InformacaoResumida)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbPaciente.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbProfissional.IdUser)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InformacaoResumida)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdMedico_Paciente }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdMedico_Paciente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdMedico_Paciente })
        </td>
    </tr>
}

</table>
